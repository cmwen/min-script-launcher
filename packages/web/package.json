{
  "name": "@min-script-launcher/web",
  "version": "0.1.0",
  "description": "Search-only web UI and REST API for discovered scripts",
  "keywords": [
    "web",
    "api",
    "express",
    "preact"
  ],
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "msl-web": "dist/server.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b && vite build",
    "prepare": "tsc -b && vite build",
    "dev": "vite",
    "test": "vitest run",
    "lint": "biome check --write src test",
    "lint:ci": "biome check src test",
    "format": "biome format --write src test",
    "format:check": "biome format src test"
  },
  "dependencies": {
    "@min-script-launcher/core": "workspace:*",
    "express": "^5.2.1",
    "preact": "^10.29.2"
  },
  "devDependencies": {
    "@types/express": "latest",
    "@types/node": "latest",
    "@types/supertest": "latest",
    "@preact/preset-vite": "^2.10.5",
    "cpy-cli": "latest",
    "supertest": "latest",
    "vitest": "latest",
    "vite": "^8.0.3"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cmwen/min-script-launcher.git",
    "directory": "packages/web"
  },
  "license": "MIT"
}
